// AUTO-GENERATED FILE - DO NOT EDIT
// This file was automatically generated by the XDK build tool.
// Any manual changes will be overwritten on the next generation.
/**
 * AUTO-GENERATED FILE - DO NOT EDIT
 * This file was automatically generated by the XDK build tool.
 * Any manual changes will be overwritten on the next generation.
 * 
 * Auto-generated structural tests for AccountActivity client.
 *
 * This module contains tests that validate the structure and API surface
 * of the AccountActivity client. These tests ensure that all expected methods
 * exist, have correct signatures, and proper type annotations for robust API contracts.
 *
 * Generated automatically - do not edit manually.
 */

import { describe, it, expect, beforeAll } from '@jest/globals';
import { AccountActivityClient } from '../../src/account_activity/client.js';
import { Client } from '../../src/client.js';

describe('AccountActivityClient Structure', () => {
  let client: Client;
  let accountActivityClient: AccountActivityClient;

  beforeAll(() => {
    client = new Client({ baseUrl: 'https://api.example.com' });
    accountActivityClient = client.accountActivity;
  });

  
  
  it('should have validateSubscription method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('validateSubscription');
    
    // Check method is callable
    const method = accountActivityClient.validateSubscription;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
      'webhookId',
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have validateSubscription method with return type annotation', () => {
    const method = accountActivityClient.validateSubscription;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  
  it('should have createSubscription method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('createSubscription');
    
    // Check method is callable
    const method = accountActivityClient.createSubscription;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
      'webhookId',
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have createSubscription method with return type annotation', () => {
    const method = accountActivityClient.createSubscription;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  
  it('should have deleteSubscription method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('deleteSubscription');
    
    // Check method is callable
    const method = accountActivityClient.deleteSubscription;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
      'webhookId',
      
      'userId',
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have deleteSubscription method with return type annotation', () => {
    const method = accountActivityClient.deleteSubscription;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  
  it('should have createReplayJob method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('createReplayJob');
    
    // Check method is callable
    const method = accountActivityClient.createReplayJob;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
      'webhookId',
      
      'fromDate',
      
      'toDate',
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have createReplayJob method with return type annotation', () => {
    const method = accountActivityClient.createReplayJob;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  
  it('should have getSubscriptionCount method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('getSubscriptionCount');
    
    // Check method is callable
    const method = accountActivityClient.getSubscriptionCount;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have getSubscriptionCount method with return type annotation', () => {
    const method = accountActivityClient.getSubscriptionCount;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  
  it('should have getSubscriptions method with correct signature', () => {
    // Check method exists
    expect(AccountActivityClient.prototype).toHaveProperty('getSubscriptions');
    
    // Check method is callable
    const method = accountActivityClient.getSubscriptions;
    expect(typeof method).toBe('function');
    
    // Check method signature by examining parameter count
    const methodString = method.toString();
    const paramsMatch = methodString.match(/\(([^)]*)\)/);
    const params = paramsMatch ? paramsMatch[1].split(',').map(p => p.trim().split(':')[0].trim()).filter(p => p) : [];
    
    // Check required parameters exist (convert to camelCase for TypeScript)
    const requiredParams = [
      
      'webhookId',
      
    ];
    
    for (const requiredParam of requiredParams) {
      // Check if parameter exists (may be in camelCase or snake_case)
      const paramExists = params.some(p => 
        p === requiredParam || 
        p.toLowerCase() === requiredParam.toLowerCase() ||
        p.replace(/_/g, '') === requiredParam.replace(/_/g, '')
      );
      expect(paramExists).toBe(true);
    }
  });

  it('should have getSubscriptions method with return type annotation', () => {
    const method = accountActivityClient.getSubscriptions;
    expect(typeof method).toBe('function');
    // TypeScript will enforce return types at compile time
    // This test ensures the method exists and is callable
  });

  

  

  it('should have all expected methods', () => {
    const expectedMethods = [
      
      'validateSubscription',
      
      'createSubscription',
      
      'deleteSubscription',
      
      'createReplayJob',
      
      'getSubscriptionCount',
      
      'getSubscriptions',
      
    ];

    for (const expectedMethod of expectedMethods) {
      expect(AccountActivityClient.prototype).toHaveProperty(expectedMethod);
      const method = (accountActivityClient as any)[expectedMethod];
      expect(typeof method).toBe('function');
    }
  });

  
});
